<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>


    <style name="FacebookLoginButton">
        <!--        <item name="android:textSize">@dimen/smallTxtSize</item>-->
        <item name="android:padding">8dp</item>
        <item name="android:background">@drawable/facebook_signin_btn</item>
        <item name="android:layout_marginTop">16dp</item>
        <item name="android:layout_marginRight">16dp</item>
        <item name="android:layout_gravity">center_horizontal</item>
        <item name="android:text">Facebook</item>
        <item name="android:layout_height">54dp</item>
    </style>

    <style name="GoogleLoginButton">
        <!--        <item name="android:textSize">@dimen/smallTxtSize</item>-->
        <item name="android:padding">8dp</item>
        <item name="android:background">@drawable/google_signin_btn</item>
        <item name="android:layout_marginTop">16dp</item>
        <item name="android:layout_marginRight">16dp</item>
        <item name="android:layout_marginLeft">16dp</item>
        <item name="android:layout_gravity">center_horizontal</item>
        <item name="android:text">Google</item>
        <item name="android:layout_height">54dp</item>
    </style>

    <dimen name="smallTxtSize">8dp</dimen>
</resources>
